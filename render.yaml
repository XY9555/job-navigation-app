services:
  - type: web
    name: job-navigation-api
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ZHIPU_API_KEY
        sync: false  # 需要手动设置